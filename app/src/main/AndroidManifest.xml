<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Khai báo các quyền cần thiết -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_green_flow"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_green_flow_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppGreenFlowGroup"
        android:usesCleartextTraffic="true">

        <!-- Cấu hình user agent cho osmdroid -->
        <meta-data
            android:name="org.osmdroid.config.USER_AGENT_VALUE"
            android:value="com.example.appgreenflowgroup" />

        <!-- Khai báo Activity khởi chạy -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppGreenFlowGroup.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Khai báo Activity khác -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppGreenFlowGroup.NoActionBar" />
    </application>

</manifest>